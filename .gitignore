**/node_modules

.venv/
lancedb/
*.pyc
staticfiles
.env
db.sqlite3
.DS_Store
.idea
node_modules

# React app build files (exclude from development but keep nlq-build for Docker)
app/swapi/build/

# Server build files and backups
server/nlq-build/
server/database.json.backup

# Test coverage files
server/coverage/

# Test scripts
test-cache-logic.sh